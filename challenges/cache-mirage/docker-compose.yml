services:
  proxy:
    image: nginx:1.27-alpine
    ports: ["127.0.0.1:8081:80"]
    volumes:
      - ./proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on: [app]
    networks: [cache]
  app:
    build: ./app
    networks: [cache]
networks:
  cache:
    internal: true
